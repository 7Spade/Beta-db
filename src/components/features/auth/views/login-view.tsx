/**
 * @fileoverview 登入頁面的主要視圖元件
 * @description 負責組合登入頁面的所有 UI 元件，形成完整的頁面。
 */
'use client';

import React from 'react';

export function LoginView() {
  return (
    <div>
      <h1>登入</h1>
      <p>Login Form 將會被放在這裡。</p>
    </div>
  );
}
