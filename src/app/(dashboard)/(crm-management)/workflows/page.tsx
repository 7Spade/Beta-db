'use client';

import { WorkflowBuilder } from '@/features/(resource-management)/(crm-management)/workflows/workflow-builder';

export default function WorkflowsPage() {
  return <WorkflowBuilder />;
}
