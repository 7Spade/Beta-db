# Firebase类型 (Firebase Types)

Firebase服务相关的TypeScript类型定义。

## 类型文件

- `auth.types.ts` - 认证类型定义
- `firestore.types.ts` - 数据库类型定义
- `functions.types.ts` - 云函数类型定义
- `storage.types.ts` - 存储类型定义
- `index.ts` - 类型统一导出

## 使用说明

定义Firebase各服务的类型接口，确保类型安全和开发体验。
